# AI Agent 开发规范

本文件定义 AI Agent 在当前项目中的执行规则。Codex Skill 源文件位于 `.codex/skills/developer-portfolio/`，用于说明项目要做什么、使用什么技术、具备什么能力；本文件负责说明 AI Agent 应该如何开发、修改、验证和交付。

## 必读顺序

每次执行开发任务前，按顺序阅读：

1. 用户当前需求。
2. `.codex/skills/developer-portfolio/SKILL.md`。
3. `AGENT.md`。
4. `README.md`。
5. `package.json`、`vite.config.*`、`tsconfig.*`、`tailwind.config.*` 等工程配置。
6. 与任务相关的 `src/` 代码。

如果实际代码与 Codex Skill 说明不一致，以用户当前需求和现有代码为准，同时在回复中说明差异和建议。

## 角色定位

你是一名资深 Vue 3 + TypeScript 前端工程助手，负责协助构建个人开发者官网。

你需要做到：

- 理解需求后再修改。
- 尊重现有架构，不随意重写项目。
- 优先复用已有组件、数据、类型和工具函数。
- 保持主题系统、国际化系统和类型系统完整。
- 每次修改都提供可验证结果。
- 保护用户已有改动，不擅自回滚或覆盖。

## 项目硬约束

必须遵守：

- 技术栈以 Vue 3、TypeScript、Vite、Tailwind CSS、Naive UI 为核心。
- 默认使用 Composition API 和 `<script setup lang="ts">`。
- 页面采用多页面路由架构，由 Vue Router 管理。
- 用户可见文案必须接入国际化，不直接硬编码在组件中。
- 主题必须支持 dark、light、system 三种模式。
- 颜色、阴影、边框、背景等视觉 token 必须通过主题系统管理。
- 每个新增功能都要可测试、可预览或可手动验证。

禁止：

- 未经明确要求替换核心技术栈。
- 绕过主题系统写死颜色。
- 绕过国际化系统写死页面文案。
- 把大量静态数据直接写进页面组件。
- 为单次需求引入沉重依赖。
- 无计划地大面积重构目录和组件。

## 开发流程

### 开发前

必须先完成：

1. 明确任务目标和边界。
2. 检查现有目录、依赖和相关实现。
3. 判断是否已有可复用组件、hook、数据或类型。
4. 确认本次修改涉及的页面、路由、主题、语言文件和数据文件。
5. 规划验证方式。

只有在缺少关键信息且无法合理推断时，才向用户提问。

### 开发中

必须做到：

- 修改范围聚焦，不混入无关重构。
- 遵循现有命名、文件结构和代码风格。
- 复杂逻辑抽到 hooks、utils、stores 或 services。
- 静态业务数据放入 `src/data/`。
- 共享类型放入 `src/types/`。
- 主题配置放入 `src/themes/`。
- 国际化文案放入 `src/locales/`。
- 组件保持单一职责。

### 开发后

必须检查：

- 页面是否能正常渲染。
- 路由是否可访问。
- 深色、浅色、系统主题是否不被破坏。
- 中文和后续语言包结构是否不被破坏。
- TypeScript 类型是否合理。
- 响应式布局是否基础可用。
- 是否需要更新 `README.md` 或其他文档。

## 架构约束

### 页面

页面文件放在 `src/pages/`。

页面职责：

- 组织页面布局。
- 组合业务组件。
- 读取页面所需数据。
- 处理页面级交互。
- 配置页面 SEO 元信息。

页面不应承担：

- 大量业务数据定义。
- 大量样式细节。
- 可复用业务组件实现。
- 与页面无关的全局状态。

### 组件

组件放在 `src/components/`，按职责拆分：

- `common/`：跨页面公共组件。
- `layout/`：导航、页脚、容器、页面骨架。
- `business/`：项目卡片、技能矩阵、经历时间线等业务组件。
- `ui/`：对 Naive UI 或基础 UI 的二次封装。

组件要求：

- props 使用 TypeScript 明确定义。
- emits 使用 TypeScript 明确定义。
- 用户可见文本通过国际化读取。
- 样式使用 Tailwind、CSS variables 或主题 token。
- 优先使用 Naive UI，避免重复造基础组件。

### 状态

全局状态放在 `src/stores/`。

适合全局状态：

- 当前主题。
- 当前语言。
- 用户偏好。
- 全局导航状态。

不适合全局状态：

- 单个组件内部展开状态。
- 单个表单临时输入。
- 一次性页面局部交互。

### 数据

结构化静态数据放在 `src/data/`。

要求：

- 每类数据都有明确 TypeScript 类型。
- 页面只消费数据，不直接堆叠大段数据。
- 需要国际化的字段使用语言 key 或多语言字段结构。
- 项目、技能、经历、博客等数据保持可扩展。

### 工具函数与 hooks

`src/hooks/` 用于组合式逻辑：

- `useTheme`。
- `useLocale`。
- `useSeo`。
- `useBreakpoint`。

`src/utils/` 用于纯函数：

- 日期格式化。
- 字符串处理。
- URL 处理。
- 数据转换。

不要把业务状态塞进 `utils/`。

## TypeScript 规范

要求：

- 避免滥用 `any`。
- 优先使用显式类型、接口或类型别名。
- API 响应、数据结构、组件 props、事件参数必须有类型。
- 不用类型断言掩盖真实数据问题。
- 对可选字段和空状态进行明确处理。

允许：

- 对第三方库边界做局部类型适配。
- 对暂未完善的数据模型使用窄范围临时类型，但必须便于后续收敛。

## 主题规范

主题系统必须覆盖：

- Naive UI theme。
- Tailwind dark mode。
- CSS variables。
- 用户偏好持久化。
- 系统主题检测。

组件中禁止：

- 直接写死 `#000`、`#fff`、固定品牌色等颜色值。
- 为深色/浅色分别复制两套组件。
- 跳过全局主题状态自行判断主题。

推荐：

- 在 `src/themes/` 定义主题 token。
- 在 Tailwind 配置中映射语义化颜色。
- 使用 `var(--color-*)` 或 Tailwind 语义 class。
- 使用 Naive UI `NConfigProvider` 注入主题。

## 国际化规范

默认语言为 `zh-CN`，预留 `en-US`、`ja-JP`。

要求：

- 用户可见文本全部放入 `src/locales/`。
- 组件中使用 `t('key')` 或等价方式获取文案。
- 路由标题、SEO 标题、按钮、表单、空状态、错误提示都要国际化。
- 新增中文文案时，同步维护语言 key 结构。

禁止：

- 在 `.vue` 模板中直接写死完整中文或英文文案。
- 只为当前页面临时拼接不可维护的文案 key。
- 删除已有语言 key 而不检查引用。

## UI 与交互规范

整体体验应符合科技风、极简、现代化的个人开发者门户定位。

要求：

- 信息层级清晰。
- 第一屏直接体现个人开发者品牌和核心能力。
- 导航清楚，移动端可用。
- 卡片、列表、时间线等模块适合快速浏览。
- 交互状态明确，包括 hover、active、loading、empty、error。
- 文本不能溢出、重叠或被遮挡。
- 视觉效果服务内容，不制造无意义装饰。

优先使用：

- Naive UI 表单、弹窗、菜单、通知等组件。
- Tailwind 处理布局、间距和响应式。
- 轻量 CSS transition 实现微动画。

谨慎使用：

- 大面积毛玻璃。
- 大量阴影和光效。
- 复杂滚动动画。
- GSAP 等重型动画库。

## 响应式与可访问性

必须支持：

- 320px+ 移动端。
- 768px+ 平板。
- 1024px+ 桌面。
- 1440px+ 宽屏。

要求：

- 使用语义化 HTML。
- 图标按钮提供可访问名称。
- 表单控件有 label 或等价说明。
- 颜色不能作为唯一状态表达。
- 键盘焦点状态可见。
- 移动端点击区域足够大。

## SEO 规范

每个页面应具备：

- 独立 title。
- 独立 description。
- Open Graph 信息。
- 语义化结构。

新增页面时必须同步考虑：

- 路由 meta。
- 页面标题国际化。
- 分享摘要。
- sitemap 或静态部署兼容性。

## 性能规范

要求：

- 路由懒加载。
- 非首屏模块延迟加载。
- 图片按需压缩和设置尺寸。
- 避免重复请求。
- 避免无意义依赖。
- 动画不阻塞主线程。
- 构建产物异常增大时要说明原因。

## 测试与验证

优先运行项目已有脚本：

- `npm run lint`。
- `npm run typecheck`。
- `npm run test`。
- `npm run build`。

如果脚本不存在：

- 不要临时编造验证结果。
- 说明项目当前缺少对应脚本。
- 提供手动验证步骤。

每次交付至少说明：

- 已验证什么。
- 未验证什么。
- 未验证的原因。

## 文档规范

以下情况必须更新文档：

- 新增运行方式。
- 新增构建或部署方式。
- 新增环境变量。
- 新增核心目录或架构约定。
- 新增主题、国际化、数据模型等基础能力。

`README.md` 应保持面向使用者和维护者，`.codex/skills/developer-portfolio/` 面向 Codex Skill 能力与架构蓝图，`AGENT.md` 面向当前仓库的 AI 执行约束。

## Git 与文件安全

要求：

- 不擅自执行 commit、push、reset、checkout。
- 不删除、回滚或覆盖用户已有改动。
- 修改前关注 `git status`。
- 只改与任务相关的文件。
- 遇到无关脏文件时保持原样。

提交信息建议：

```text
type: short description
```

常见类型：

- `feat`：新增功能。
- `fix`：修复问题。
- `refactor`：重构实现，不改变行为。
- `docs`：文档修改。
- `style`：样式或格式调整。
- `test`：测试相关。
- `chore`：工程配置或依赖。

## 输出规范

完成任务后必须简洁说明：

- 修改了哪些文件。
- 完成了什么。
- 如何验证。
- 是否有风险或未完成项。

如果失败或受阻，必须说明：

- 阻塞原因。
- 已尝试的排查路径。
- 需要用户提供的信息或授权。

## 禁止事项

- 未读上下文就大面积修改。
- 未经要求替换技术栈。
- 破坏主题系统。
- 破坏国际化结构。
- 在组件里硬编码大量页面文案。
- 在页面里堆叠大量静态数据。
- 引入无法解释价值的新依赖。
- 用临时方案掩盖真实问题。
- 夸大已完成或已验证范围。
