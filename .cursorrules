# 智能生成提交

当 Cursor 通过源代码管理面板的「生成提交信息」按钮、Agent 或其它方式为本仓库写 Git 提交信息时，必须遵守：

- 全文使用简体中文，不要用英文写摘要或正文。
- 格式：

```text
type: 中文摘要

- 改动：说明做了什么、为什么改
- 文件：列出每个被修改、新增或删除的文件路径
```

- type 仅从 feat、fix、refactor、docs、style、test、chore、perf、build、ci 中选择。
- 标题只写一行，概括改动目的。
- 正文必须写清改了什么，不能只写空泛摘要。
- 正文必须逐条列出本次涉及的全部文件路径，不要遗漏。
- 新增、修改、删除的文件都要写出来。
- 专有名词可保留原文，例如 Vue、Vite、Naive UI、TypeScript。
- 不要夸大范围；纯文档改动用 `docs`。
- 不要写入密钥、token 或敏感信息。

正确示例：

```text
feat: 新增首页项目卡片与主题适配

- 改动：首页增加项目卡片，并接入深浅色主题
- 文件：
  - src/pages/Home.vue
  - src/data/home.ts
  - src/locales/zh-CN.ts
```

错误示例：

```text
feat: add project cards to homepage
```

```text
feat: 新增首页项目卡片
```
