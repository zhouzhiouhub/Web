---
description: Git 提交信息必须使用简体中文
alwaysApply: true
---

# Git 提交信息

为本仓库生成、建议或实际写入 Git 提交信息时，必须使用简体中文。

## 格式

```text
type: 中文摘要
```

- `type` 使用：`feat`、`fix`、`refactor`、`docs`、`style`、`test`、`chore`、`perf`、`build`、`ci`
- 标题与正文都用简体中文
- 标题写清改动目的，不要罗列文件名
- 标题只写一行，尽量不超过约 50 个汉字
- 仅在改动不直观时补充简短中文正文
- Vue、Vite、Naive UI、TypeScript 等专有名词可保留原文
- 不要写入密钥、token 或敏感信息

## 示例

```text
feat: 新增首页项目卡片与主题适配
```

```text
fix: 修复移动端导航遮挡内容
```

不要写：

```text
feat: add homepage project cards
```
