---
description: Git 提交须用中文，并写清改动内容和文件
alwaysApply: true
---

# Git 提交信息

为本仓库生成、建议或实际写入 Git 提交信息时，必须使用简体中文，并且必须写清「改了什么」和「改了哪些文件」。

## 格式

```text
type: 中文摘要

- 改动：说明做了什么、为什么改
- 文件：列出每个被修改、新增或删除的文件路径
```

- `type` 使用：`feat`、`fix`、`refactor`、`docs`、`style`、`test`、`chore`、`perf`、`build`、`ci`
- 标题与正文都用简体中文
- 标题只写一行，概括本次改动目的
- 正文必须写清改了什么，不能只写空泛摘要
- 正文必须逐条列出本次涉及的全部文件路径，不要遗漏
- 新增、修改、删除的文件都要写出来
- Vue、Vite、Naive UI、TypeScript 等专有名词可保留原文
- 不要写入密钥、token 或敏感信息

## 示例

```text
feat: 新增首页项目卡片与主题适配

- 改动：首页增加项目卡片，并接入深浅色主题
- 文件：
  - src/pages/Home.vue
  - src/data/home.ts
  - src/locales/zh-CN.ts
```

不要写只有英文标题、或只有摘要却不列文件的提交信息。
